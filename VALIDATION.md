# 自学资料验证记录

## 2026-09-10：扩写原课程正文、33节连续带练与四本完整Notebook

本轮直接扩写原11门工程正文，每门增加3节连续带练，共33节。内容包含逐步计算、原始字节、状态迁移、故障过程、修改任务与解析。工程正文当前约5.6万字符，计数包括原概述，不将字符数等同于教学质量或授课时长。课程总表与各门首页直接进入带练，避免只浏览概述。

- 新增Notebook13–16：CAN FD、EtherCAT、电机控制、系统联调，每本17个单元，其中9个代码单元含环境准备。四本在各自新内核完整执行，共36个代码单元全部通过。
- 将四本新课的实际默认输出保存到教材Notebook中，源码与执行版本逐单元内容一致。保留数值输出和8幅PNG图，人工查看PI响应/积分/力矩对照和EtherCAT周期时间线，轴、图例与主要内容显示清楚。控件另提供默认显式函数调用，非交互阅读也能看静态结果。
- 新增`labs/workshop_lab.py`，仅实现自定12字节协议、通道路由/序号、只读驱动决策、简化速度PI、停止距离、标量标定与插值等离线模型。没有设备连接或发送接口。
- `python -m unittest discover -s tests -v`：81项通过，其中本轮15项覆盖独立黄金字节、有符号极值、截断、重复不刷新、回绕、跨节点/时间异常、启动门禁、运行条件丢失、积分恢复、独立标定与禁止外推。
- 本地链接检查：82份Markdown、17本Notebook、857处相对路径，断链0处。另检查18处指向新增`worked-lessons`显式锚点的链接，均存在；没有将外部网站可用性包含在路径检查中。
- 11门正文中的15个表格、107行列数一致；33节标题数量与每门3节核对通过。新增Python和Notebook源文件语法、UTF-8文本检查通过；补算RMS载荷与创新标准化等手算例子。
- 17本Notebook格式验证通过。只修改原00首页的Markdown入口，保留其原代码、输出和执行计数；01–12未重建或重新执行。本轮执行报告位于`outputs/executed_notebooks/report_selected.json`，内容检查位于`outputs/workshop_content_check.json`。

本轮未进行CAN FD/EtherCAT、电机或标定硬件实测。教学协议不是厂家协议，驱动决策表不是完整设备状态机，速度模型不含电流环/重力/间隙，停止距离也不是实物保护距离。新Notebook保存的是离线默认运行结果，学员改参数后必须重新运行，不能沿用旧输出作为验证证据。

## 2026-09-10：全门类工程课程与CAN FD/EtherCAT独立课程

新增11门工程正文：数学与误差、本体与电气、接口与嵌入式网络、CAN FD、EtherCAT、运动学与标定、控制与实时执行、碰撞规划与工作空间、感知估计、AI策略、软件交付。另有总路线、工程实验说明、统一报告和阶段考核，共15份新增Markdown、约3.7万字符。原七天课保留为基础层，新增各天与各门手册到工程层的导航。

- `python -m unittest discover -s tests -v`：66项通过，包括本次新增13项工程实验测试。覆盖FD合法长度/填充与拒绝超长、PDO有符号解码/截断、状态位掩码、WKC异常不发布输入、周期违例、雅可比差分、薄障碍漏检、episode泄漏和故障后不自动重启。
- `python labs/engineering_lab.py all`：8类合成离线实验全部完成，结果保存在`outputs/engineering_all.json`；分别记录模型假设与未实现的设备行为。
- 本地链接检查：82份Markdown、13本Notebook、818处相对链接，断链0处；不验证外部网站或章节锚点。
- 新增文档18个表格、137行列数一致；Python示例与新增Python文件语法检查通过。检查并修复了PowerShell管道编码造成的新增导航乱码，最终相关课程、手册与Notebook未发现连续问号或Unicode替换字符。
- 课堂首页仅追加工程课程Markdown导航，源码入口同步更新；对照确认原代码单元、执行计数和输出未改变，Notebook格式验证通过。未重建或重新运行其他Notebook。
- 静态读取创芯/周立功CAN FD配置及WHJ交换实现，区分仲裁/数据速率、发送BRS标志和阻塞收发。EtherCAT内容按IgH、Beckhoff及ETG资料组织，未把工作区未找到的主站配置写成团队已部署方案。

此次没有连接CAN FD或EtherCAT设备，没有运行SocketCAN/IgH硬件命令、激活TwinCAT工程、使能电机或改写驱动/固件。ESI、设备型号、针脚、时序、PDO映射、限值和停止行为须在对应工位确认；教学PDO与WKC是明确假设，不能直接用作实物配置。个别外部PDF直接获取失败，未将全量外链写成已验证可用。

## 2026-09-10：接线、单电机调试与控制实操衔接

新增实操路线、接线/单电机/控制三张操作卡和工位交付表。首页、七天路线、自学说明、第2/3/4/7天正文及物理连接和控制手册接入实操入口；术语表补充现场用语，操作卡在首次使用处解释术语。实操成果包含逐针接线、只读反馈、有限运动、停止和参数对照。

- 本地链接检查通过：67份Markdown、13本Notebook、689处相对链接，断链0处；不检查章节锚点和外部网站。
- 新增5份文档及更新的术语表，共13个表格、149行，列数一致；这些文件未发现Unicode替换字符。
- 编码器计数到输出轴角度、两只120Ω并联、超调百分比算例复算通过。
- 实际执行`python labs/algorithm_lab.py --help`和`python labs/algorithm_lab.py pid`，后者完成1200个教学采样并输出`outputs/algorithms_pid.json`。没有重新运行未修改的全部算法测试。
- 只读核对`rmd_motor`的接口声明及`cxt_can_rmd_motor`的收发实现和Windows旧示例，记录其命名差异及自动速度指令；未将旧示例标为可直接运行的实物操作程序，也未修改驱动代码。

本次为教材和操作流程补充，未进行真机接线、运动、停止或参数验证。实际针脚、电气参数、负载限制、工具与固件匹配关系必须填入工位表并现场核实；离线模型参数不能充当实物推荐值。CAN终端说明参考TI官方资料，未核验所有既有外链。

## 七天主线整理与暂缓主题补课

首页、七天路线、自学说明、深度地图、七篇正文和课堂首页统一导航。主线采用每天约3小时的同一学习顺序，增加每天的先修衔接、具体成果和过关题；第二天取消先学完整矩阵的入口要求。新增扩展选课表与第17章，覆盖原表全部暂缓主题，配有本地解释、算例、自检、答案及参考阅读。

- 本地链接检查覆盖62份Markdown与13本Notebook，651处相对路径均存在；该检查不判断外部网站可用性。
- 对本次13份课程文档另查11个表格的列数一致性，以及40处指向主线/扩展小课堂的显式章节锚点，均通过。
- 13本Notebook格式验证通过。只重整00课堂首页的Markdown；生成前后逐个比较其代码单元源码一致，并保留原代码单元输出。其余12本未重建，也未重复运行未变实验。
- 本次文档3个Python代码段通过语法检查；新增类示例实际输出`0.25 None`。惯量、通信时间、阻尼更新、采样概率、FK与训练损失的算例通过数值复算，两层网络的梯度另用中央差分核对。
- 新增一般2×2逆矩阵例子用正向和逆向手算核对。学习流程与先修关系使用Mermaid，并提供文字说明，阅读不依赖图形渲染。

本次数值检查记录位于本机`outputs/learning_route_check.json`。修改范围为教材与导航，没有改动算法实现、播放器或硬件配置；此前53项算法测试与动画运行记录保留在下方，本次不把它们写成重新执行的结果。外部资料使用Python官方文档、Modern Robotics、MIT课程、LaValle与D2L等原始来源；未验证所有外链，也未进行真机或平台部署。


## 基础衔接、物理连接与OpenMAIC参考方案补充

本次新增第14至16章：物理接线与链路；矩阵、坐标、变换顺序与FK；全课程前置与深入参考。新增18道含解析的诊断题，补充7个逐步演示和2本可编辑实验；课程现有13本Notebook、16个演示主题。

- 核心测试53项全部通过，新增10项覆盖独立手算矩阵、维度错误、点与方向、变换顺序、逆平移、反射拒绝、FK三角核对、UART位向量、SPI时间和终端并联。
- Notebook11与12在各自新内核中完整执行，分别6与4个代码块全部通过，记录另存为 `outputs/executed_notebooks/report_frames_physical.json`。原笔记仅有导航/主题数量的文字变动，没有重跑全部旧实验。
- Edge测试覆盖16主题316帧，SVG解析、播放/暂停、前后单步、切换/跳转、全部帧文字边界通过，无JavaScript异常。实际查看了矩阵、坐标、UART等截图，数字、图形和中文显示正常。
- 本地路径检查覆盖60份Markdown和13本Notebook；外部资料列为参考，路径检查不证明每个外部站点可用。
- OpenMAIC教学素材包包含17份选定源文件与清单，ZIP完整性和各文件SHA-256核对通过。它不是平台原生课堂存档；未进行平台导入、运行、模型调用或多人会话验收。

OpenMAIC核查仅针对用户指定仓库的main README，读取哈希、声明要求和未验证范围保存在integrations/openmaic/source_review.json。没有复制平台技能或第三方包，没有上传团队资料、配置密钥或产生模型调用费用。

本次通信算例为离线教学；引脚、电压、参考地和终端需要对应硬件资料。本次矩阵库为小型教学实现，未声称实现完整空间运动学、POE或6维twist框架变换。


## 2026-09-08：协议栈、IK家族、工具链与全课程深入

新增4篇深入手册、3本Notebook和20道含解析的扩展题；七天正文、原手册、课程首页和工具说明已连接到新的专题入口。下方保留此前验证记录，测试数量按各次验证的实际范围记录。

| 核验 | 本次结果 |
|---|---|
| 核心测试 | `python -m unittest discover -s tests -v`，43项通过；新增TCP任意切分、错误长度、CAN仲裁、IK分支平均、检索初值与延迟模型检查 |
| 新增Notebook | 08、09、10各自在新内核中完整运行，分别5、6、4个代码块，共15个通过；本次未重复执行代码未变的原8本 |
| Notebook总量 | 11本；00首页仅新增深入阅读导航，其余原课程代码未改 |
| 可编辑实验 | PCAP逐层解析、TCP长度分帧、CAN ID仲裁、代数IK、近邻初值+DLS、局部近似、延迟及滤波对照均实际运行 |
| 离线演示 | 9主题244帧SVG均可解析；本机Edge实测播放、暂停、前后单步、主题切换、进度跳转通过；全部244帧文字边界检查通过，无JavaScript异常 |
| 视觉复核 | 查看协议栈、IK多解、Jacobian、A*和碰撞覆盖的浏览器截图，中文、数值与图形可读；4张静态SVG与播放器共用生成帧 |
| 本地链接 | 52份Markdown和11本Notebook，492处本地链接全部存在；不代表所有外部站点或锚点都已验证 |
| Python语法 | tools、labs、tests及Notebook源文件共30个Python文件通过AST解析 |

本次Notebook结果保存在 `outputs/executed_notebooks/report_selected.json`，原8本的记录仍为同目录 `report.json`。演示器检查脚本为 `tools/check_concept_player.py`，报告和9张主题截图在 `outputs/concept_player_check/`；这些outputs产物是本机验证记录，不随课程源文件提交。

复跑新增实验：`python tools/check_notebooks.py 08_protocol_stack.ipynb 09_ik_families.ipynb 10_visual_reasoning.ipynb`。重建演示和静态图：`python tools/build_concept_player.py`。浏览器检查额外需要Playwright和本机Edge，学员直接打开HTML无需安装它们。

数据驱动IK使用425条教学样本和30个留出目标，实际实现为检索初值与DLS精修；IKFlow等神经网络方法只作论文导读。CAN演示限11位标准ID仲裁阶段；TCP实验为本地字节流解析，不接真实网络设备。碰撞图是二维矩形与近似圆，不表示已验证团队机器人网格或折叠姿态。

GitHub/GitLab/Gitea、容器及CI内容是操作讲义和模板，本次未执行远端建库、PR、CI或部署；CI示例仍放在templates中。没有运行真机、GPU策略训练或论文完整复现。

## 此前验证记录

日期：2026-09-07，Windows / Python 3.10。

交互课堂追加验证：2026-09-07至08。8本Notebook在各自新内核中执行，共43个代码块全部成功，无错误输出；结果保存在 `outputs/executed_notebooks/report.json` 和执行后的笔记副本中。第七天实际调用算法脚本并运行核心测试，35项通过。

2026-09-08使用本机Edge进行浏览器实测：带令牌访问Jupyter、选择Training Python、执行入门笔记、将300修改为450并得到 `450 mm = 0.450 m`、保存后复读文件确认改动、滑块从30°变为31°且Python生成新的图像，全部通过。截图已人工查看，中文、代码、计算结果与曲线显示正常。记录在 `outputs/browser_smoke/report.json`；示例图收录于交互课堂说明。验证后关闭测试服务；未部署公共网站。

启动器使用独立的会话运行目录，避免Windows普通账户与隔离测试账户复用具有不同访问权限的Jupyter登录文件。这个调整也在上述浏览器实测中验证。

交互环境使用项目 `.venv`：JupyterLab 4.6.3、ipykernel 7.3.0、nbclient 0.11.0、nbformat 5.10.4、ipywidgets 8.1.7、jupyterlab-widgets 3.0.15、NumPy 1.24.4、Matplotlib 3.10.9。本机环境复用了已安装的系统包，另外将widget前端扩展安装到项目环境；文档中的新环境安装步骤不依赖这些本机路径。

`python tools/check_learning_links.py` 覆盖43份Markdown和8本Notebook中的本地链接；最终检查347处链接时均存在。新增七天实验、工程细节与课程入口已经互相连接。命令行链接检查只验证路径存在，不验证每个网页或段落锚点。

| 验证 | 结果 |
|---|---|
| `python labs/course_lab.py all` | 六个基础实验运行成功 |
| `python labs/algorithm_lab.py all` | 五个算法实验运行成功，IK收敛、A*代价19、轨迹2.5秒 |
| `python -m unittest discover -s tests -v` | 35项测试通过，含参考数据校验 |
| Markdown相对链接 | 41份Markdown检查时无失效本地链接；图示路径均存在 |
| 图片 | 14组PNG/SVG已生成，抽查两连杆、雅可比、感知链与PID图的中文显示 |
| 教学数据 | pcap含4帧，IPv4长度与校验正确；SPI上升沿数据为A5 |
| 教学URDF | XML可解析，2个转动关节、1个固定工具关节 |
| 原生窗口 | 点击目标、切换分支、环外目标和滑块重绘的隐藏窗口检查通过 |
| 新增参考数据 | 9类教学CSV、4模型24关节声明快照；清单哈希、行数及关键独立预期通过检查 |
| 零基础内容 | 七天正文重写，补充数学台阶、操作卡、算法故事和21道就地自检题 |
| 新增动画与分镜 | 12段GIF共480帧均可解码，每段7.32秒；12张四步PNG分镜齐全，抽查坐标、两连杆与A*排版 |
| 动画播放器 | 隐藏窗口检查通过：载入40帧、前后单步、播放、暂停与主题切换；采用下述本机Tcl/Tk测试环境 |

本机原有Tcl脚本路径无法直接初始化，因此窗口检查使用复制到工作区build目录的同一套已安装Tcl/Tk运行时，并仅在测试进程中设置TCL_LIBRARY/TK_LIBRARY。没有把该绝对路径或运行时加入课程依赖。普通Python安装需带可用Tcl/Tk；故障处理见 [工具说明](TOOLS.md)。

未执行真机控制、GPU求解、完整机器人策略训练或Wireshark界面操作。pcap使用独立字节解析检查，基础自学也可直接读CSV。算法讲解中没有本地实现的项目在 [算法地图](handbook/ALGORITHMS.md) 明确标注。
