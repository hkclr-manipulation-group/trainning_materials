# 机械臂设计开发：从选型到样机验收

[课程首页](README.md) · [工程课程](ENGINEERING_PATH.md) · [团队项目地图](PROJECT_MAP.md) · [方案评审模板](templates/ROBOT_DESIGN_REVIEW.md)

适合已经了解关节、坐标和基本控制，准备参与 Spark2、Dual 或新机械臂设计的同事。目标是把现有库连成可复现的工作流程：同一个设计版本，从需求和关节选型一直追踪到模型、分析、样机和实测结果。

本文是团队流程建议与综合实践材料。模型发布工具、统一报告流水线和设计工作台是待建设项，不表示已经实现；现有工具和待补能力在下文分别列出。资料核对日期：2026-09-14。

## 1. 一条主线，各阶段交付什么

需求与工况 → 关节和连杆方案 → CAD与模型包 → 负载、碰撞和工作空间分析 → 样机调试 → 标定与验收 → 反馈到下一版设计。

| 阶段 | 需要的输入 | 要做的事 | 留下的证据 | 建议牵头角色 |
|---|---|---|---|---|
| 需求 | 使用任务、空间、工具和工件 | 明确负载及重心、臂展、速度、工作周期、精度目标和预算 | 需求表、典型姿态与动作清单 | 产品与系统 |
| 初步方案 | 关节目录、需求表 | 比较关节组合、杆长、材料、臂重和成本 | 至少两套方案及采用/放弃理由 | 机械与控制 |
| 机械设计 | 初步方案、装配约束 | 建立CAD，核对质量、重心、惯量、轴方向、限位、TCP | 可追溯的CAD与模型包 | 机械 |
| 离线分析 | 同版模型包与统一工况 | 计算扭矩需求、碰撞和工作空间，记录误差与适用范围 | 分析输入、环境、结果及失败项 | 算法与控制 |
| 样机调试 | 已确认的硬件配置 | 完成接线、反馈、有限运动、停止与控制调试 | 工位记录、日志、固件与参数版本 | 电气、嵌入式与控制 |
| 标定与验收 | 样机、工具、测量方案 | 测负载表现、跟踪、温升与重复定位，核对预测 | 原始测量、测试报告、偏差原因 | 测试与系统 |
| 下一版 | 预测与实测差异 | 更新参数依据、结构或控制方案，保留旧版 | 新版本及变更影响说明 | 对应模块负责人 |

每个阶段回答四个问题：输入是哪一版？哪些数是估算？哪些已经验证？下一阶段能否用这些输入复现？不以“生成了图”或“程序运行成功”代替工程结论。

## 2. 先解决模型版本问题

当前已有设计、模型、SDK、碰撞与工作空间库。独立运行很有价值，但手工复制后可能出现同名模型对应不同结构的情况。近期内部球接入工作空间时，就发现了四个 Dual 预设与球生成来源的运动学版本不一致；完整 Dual 2.2 还存在旧连杆名残留。案例记录见工作空间项目的[迁移报告](../robot_workspace_dexterous/models/interior_sync_report.json)和[使用说明](../robot_workspace_dexterous/README.md)。

建议明确唯一的设计来源与发布责任：机械设计来源由设计人员维护，模型发布入口优先整合到现有配置工具中；各下游库消费明确版本的导出包。可以保留本地副本，但不能只依靠文件夹名字判断版本。

一个模型包至少应记录：

| 内容 | 应记录的信息 |
|---|---|
| 身份 | 型号、设计修订号、生成时间、来源提交或快照校验值 |
| 机械参数 | 每轴关节型号、杆长、整机尺寸、质量、重心、惯量及来源 |
| 坐标与接口 | 基座、轴方向、关节零位、限位、TCP、夹爪或手的版本 |
| 几何与碰撞 | CAD/网格版本、URDF、球文件、拟合方式、扩张量、忽略对及依据 |
| 验证 | 校验结果、分析环境、工况、已知限制、对应报告 |

这是建议的数据契约，不是现有统一配置格式。落地时应兼容已有URDF、YAML和JSON，不让下游再次手工维护同一组长度或关节参数。

**设计模型与样机标定参数也要分开记录。** 同一设计的不同序列号可以有不同零位、TCP和标定结果，但都应能追溯到对应的机械设计。

## 3. 现有库如何分工

以下项目链接依赖同级工作区；只复制本课程时，正文和模板仍可使用。

| 工作 | 现有入口 | 整合方向 |
|---|---|---|
| 厂家资料、关节选型、初步负载估算 | [robot_catalog](../robot_catalog) | 扩展方案保存与对比，记录厂家参数来源、日期和未知项 |
| CAD与几何模型 | [spark2_design](../spark2_design)、[cuarm_mesh](../cuarm_mesh/readme.md) | 统一导出约定，标明装配修订号和坐标含义 |
| 型号配置 | [cuarm_configuration](../cuarm_configuration/README.md) | 优先作为模型发布入口的整合位置，减少重复配置 |
| 碰撞拟合与查看 | [collision_shpere_generation](../collision_shpere_generation/README.md) | 输出球文件、拟合报告和[预览图册](../collision_shpere_generation/examples/interior_gallery.html) |
| 可达与灵巧工作空间 | [robot_workspace_dexterous](../robot_workspace_dexterous/README.md) | 使用同版模型；统一网格、方向采样、工具和碰撞设置后比较 |
| 控制与应用 | [cuarm_panel_control](../cuarm_panel_control/README.md)、[spark2_sdk](../spark2_sdk/README.md) | 关联控制配置、固件、SDK与模型版本 |
| 标定与验收 | [calibration](../calibration)、[spark2_auto_test](../spark2_auto_test/README.md) | 把原始记录、报告、标定参数与样机序列号关联起来 |

不要求把所有代码合并成一个仓库。先统一输入、版本和输出，再让一个入口调用现有工具。

## 4. 还需要哪些工具

| 优先级 | 需要补的能力 | 建议落地方式 | 做到什么算完成 |
|---|---|---|---|
| 第一阶段 | 模型发布与版本检查 | 扩展现有配置/导出工具 | 下游能确认来源、校验值和型号；错配、无效连杆与非法参数会明确失败 |
| 第一阶段 | 一键离线检查与统一报告 | 调用现有CPU校验、负载、碰撞与工作空间工具 | 每项显示通过、失败、未运行或估算；结果绑定输入版本 |
| 第一阶段 | 方案对比页面 | 扩展目录中的设计分析界面 | 同屏比较关节组合、杆长、整机长度、臂重、负载、成本和数据缺项 |
| 第二阶段 | 动态负载校核 | 先盘点已有动力学实现，缺少时接入成熟算法 | 在相同姿态/轨迹、载荷重心、速度和加速度下比较扭矩需求 |
| 第二阶段 | 结构刚度与热分析 | 优先复用现有CAD分析工具 | 有材料、边界条件、网格与结果；解释变形、温升与试验差异 |
| 第二阶段 | 实测结果回写 | 扩展自动验收与报告工具 | 能比较同一设计不同样机、同一样机不同版本的结果 |

动力学候选工具可考虑 **Pinocchio**：其逆动力学与惯性矩阵算法可用于计算关节扭矩需求；它不会自动替你确定电机热额定值和真实摩擦参数。[官方算法说明](https://docs.ros.org/en/ros2_packages/rolling/api/pinocchio/doc/a-features/g-dynamic.html)

如果现有CAD没有合适的分析能力，可评估 **FreeCAD FEM + CalculiX**，用于结构与热机械分析。材料、载荷和约束仍需工程人员确认，碳纤维也不能直接套用铝的材料参数。[官方说明](https://github.com/FreeCAD/FreeCAD-documentation/blob/main/wiki/FEM_CalculiX.md)

团队已有Isaac Sim导入资料，先接通现有仿真流程。课程中的MuJoCo仍可用于选学与对照实验，不因此要求设计团队再迁移一套仿真平台。以上候选工具不属于本课程必装依赖，也未在本次整合中安装或接入。

## 5. 结果必须使用同一种口径

### 负载与选型

10、30、60、90 Nm可以作为团队常用筛选档位，但必须明确数字指的是额定、连续还是峰值扭矩，以及对应转速与持续时间。目录缺失的数据标为未知，不能填0。

负载说明至少包括工具和工件质量、重心位置、姿态、连杆与连接件质量、动作速度和加速度。静态重力计算适合初筛；动作加减速、工作周期和热约束需要进一步校核。对比时同时显示各段杆长、参考姿态尺寸和臂展定义，不能把杆长之和直接当成所有姿态的工作范围。

### 内部球与干涉

当前碰撞库和工作空间的11个预设已接入内部球，默认半径扩张为0 mm。内部球可能留下未覆盖区域，开口网格还包含明确标注的内部估算。**0 mm表示不额外扩张半径，不表示拟合误差为0。** 图中的最大采样间隙也不是连续表面的严格误差上界。

球模型用于快速分析，最终干涉验收还需核对CAD/网格几何、关节运动范围和关键姿态。球文件更新后，旧工作空间图不会自动更新。忽略对必须有结构依据，不能为了得到更大的工作空间而直接关闭碰撞。

### 精度与重复性

目录中的回差叠加用于理论误差估算；要结合当前姿态、轴方向和到末端的距离，而不是把角度直接作为毫米相加。标注该结果的假设，不将其命名为实测重复定位精度。

SDK/编码器反馈能够反映反馈位置的重复性，但通过同一个运动学模型计算出的末端位置，不能独立验证模型之外的结构变形和真实TCP误差。对外报告实际末端重复定位表现时，应记录外部测量方法、工具负载、姿态、温度、循环次数和统计口径。

## 6. 综合实践：比较两个机械臂方案

先复制[方案评审模板](templates/ROBOT_DESIGN_REVIEW.md)，选择一个团队现有型号作为A方案。B方案只改变一个因素，例如一段杆长、材料或一个关节型号，先在方案表中写出预期变化。没有生成匹配的B模型前，不把A的工作空间图作为B的结果。

1. 记录任务、质量与重心、速度/工作周期、尺寸限制，以及所有输入的来源。
2. 用相同工况比较A/B的总长定义、臂重、静态扭矩需求、关节裕量和成本；未知项单独列出。
3. 有匹配模型后，检查模型与球文件版本，再运行离线分析。保存原始结果和命令，而不只截一张图。
4. 区分“CPU输入检查通过”“GPU工作空间计算完成”“结构校核完成”“样机实测完成”。未运行就填未运行。
5. 写出采用方案、主要风险、下一步需要补的证据；由同事按记录复现一次。

已有工具可以在各自环境中运行。下面两个命令在 **robot_workspace_dexterous 项目根目录**执行；课程本身不要求安装其依赖：

```text
python run.py --config configs/spark2_v2.yaml --validate-only
python run.py --config configs/spark2_v2.yaml --diagnose-only --diagnostic-samples 32
```

第一条检查输入，第二条做确定性随机关节姿态的CPU碰撞诊断；它们均不运行GPU IK，也不控制硬件。真正的工作空间计算按对应项目README在合适的CUDA环境中执行。实际工位操作继续遵循[实操路线](PRACTICAL_PATH.md)，不由离线流水线自动启动真机运动。

## 7. 自检与答案

**题1：两个文件夹都叫Spark2 v2，是否可以把其中一个的球文件复制给另一个直接计算？**

不能仅凭名字判断。要核对模型修订、连杆与坐标、工具配置和球文件来源；文件校验值只能证明文件内容一致，还需确认这些文件确实属于同一版模型。

**题2：内部球扩张0 mm，CPU抽样无碰撞，是否说明机械结构不存在干涉？**

不能。球集合可能漏掉外形，抽样也没有穷尽运动范围；需进一步核对关键姿态和实际几何。

**题3：B方案换成更大的峰值扭矩关节，是否一定能提高连续负载和重复定位表现？**

不一定。还要检查连续扭矩、转速、热与工作周期、自重增加、结构变形及回差；实测重复定位结论需要相应测量证据。

## 8. 实施顺序

第一步先选择一个型号试点，约定模型包内容和版本规则，做出能拒绝已知错配的检查。第二步接通现有工具与统一报告，复现一条完整的离线分析链。第三步增加方案对比界面。随后再按具体项目需要补动态负载、结构分析和测试回写。

最终工作台的操作可以很简单：**选择型号与版本 → 修改方案 → 运行检查 → 对比报告 → 导出模型包**。这描述的是建设目标；当前真实可用入口仍以各项目README为准。
